<p id="notice"><%= notice %></p>

<h1>Providers</h1>

<table>
  <thead>
    <tr>
      <th>Corporatename</th>
      <th>Fantasyname</th>
      <th>Phone</th>
      <th>Cnpj</th>
      <th>Stateregistration</th>
      <th>Email</th>
      <th>Type</th>
      <th>Codeprovider</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @providers.each do |provider| %>
      <tr>
        <td><%= provider.corporateName %></td>
        <td><%= provider.fantasyName %></td>
        <td><%= provider.phone %></td>
        <td><%= provider.cnpj %></td>
        <td><%= provider.stateRegistration %></td>
        <td><%= provider.email %></td>
        <td><%= provider.type %></td>
        <td><%= provider.codeProvider %></td>
        <td><%= link_to 'Show', provider %></td>
        <td><%= link_to 'Edit', edit_provider_path(provider) %></td>
        <td><%= link_to 'Destroy', provider, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Provider', new_provider_path %>
